function JSXRules() {
    return (
      <div>
        <h1>JSX Rules</h1>
        <p>Here are some important RULES of JSX:</p>
        <ul>
          <li>JSX must return a <strong>single parent element</strong>.</li>
          <li>JSX elements must be <strong>properly closed</strong>.</li>
          <li>JSX attributes are written using <strong>camelCase</strong> (e.g., <code>className</code> instead of <code>class</code>).</li>
        </ul>
      </div>
    );
  }
  
  export default JSXRules;
  